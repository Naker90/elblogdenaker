{
  "name": "elblogdenaker",
  "version": "0.0.1",
  "description": "Blog personal",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy:index": "cp ./src/index.html dist",
    "copy:styles": "cp -a ./src/styles dist",
    "build": "npm run copy:index && npm run copy:styles && webpack",
    "dev": "npm run build && webpack-dev-server"
  },
  "author": "Antonio Sánchez",
  "license": "MIT",
  "devDependencies": {
    "path": "^0.12.7",
    "webpack": "^4.6.0",
    "webpack-cli": "^2.0.15",
    "webpack-dev-server": "^3.1.3"
  }
}

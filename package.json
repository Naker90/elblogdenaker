{
  "name": "elblogdenaker",
  "version": "0.0.1",
  "description": "Blog personal",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy:index": "cp ./src/index.html dist",
    "copy:styles": "cp -a ./src/styles dist",
    "copy:images": "cp -a ./src/images dist",
    "watch:build": "watch 'npm run copy:index && npm run copy:styles && npm run copy:images && webpack' src",
    "dev": "npm run build && webpack-dev-server"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Naker90/elblogdenaker.git"
  },
  "author": "Antonio Sánchez",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "path": "^0.12.7",
    "watch": "^1.0.2",
    "webpack": "^4.6.0",
    "webpack-cli": "^2.0.15",
    "webpack-dev-server": "^3.1.3"
  }
}
